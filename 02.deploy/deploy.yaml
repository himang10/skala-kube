apiVersion: apps/v1
kind: Deployment
metadata:
  name: skala-webserver
  namespace: class-0
  labels:
    app: skala-webserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: skala-webserver
  template:
    metadata:
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '8080'
        prometheus.io/path: '/actuator/prometheus'
        update: 1639bd93
      labels:
        app: skala-webserver
    spec:
      containers:
      - name: webserver
        image: skala-registry.skala-ai.com/class-0/skala-webserver:1.0
        imagePullPolicy: Always
        env:
        - name: USER_NAME
          value: skala
        - name: NAMESPACE
          value: class-0
        - name: SPRING_PROFILES_ACTIVE  
          value: "local"  
