apiVersion: v1
kind: Service
metadata:
  name: skala-webserver
  namespace: class-0
spec:
  selector:
    app: skala-webserver
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP


